<%= render 'shared/error_messages', :object => f.object %>
<!--This creates a variable called object in the partial, which we can then use to generate the error messages-->
<!-- -it renders the partial '_error_messages.html.erb'-->
<div class="field">
	<%= f.label :name %><br />
	<%= f.text_field :name %>
</div>
<div class="field">
	<%= f.label :email %><br />
	<%= f.text_field :email %>
</div>
<div class="field">
	<%= f.label :password %><br />
	<%= f.password_field :password %>
</div>
<div class="field">
	<%= f.label :password_confirmation, "Confirmation" %><br />
	<%= f.password_field :password_confirmation %>
</div>